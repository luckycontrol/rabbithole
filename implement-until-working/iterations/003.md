# Iteration 003

## Summary
- Status: complete
- Goal: Remove the final race from the card IME regression and prove the full Korean question is retained and sent exactly once.
- Why this iteration: Iteration 002 reached all product behavior assertions; only a transient title assertion failed after the immediate mock answer replaced `테스트` with `Card IME`.
- Method: Identify the newly appended card by the pre-submit node count and assert its stable `.origin-quote` exactly matches `테스트`.
- Compared with the previous baseline: better; the sole failing acceptance test and the complete repository suite now pass.

## Context Read
- `AGENTS.md`: reviewed before edits.
- `implement-until-working/task.md`: reviewed before edits.
- `implement-until-working/method-history.md`: reviewed before edits.
- `implement-until-working/iterations/001.md` and `002.md`: reviewed before edits.
- Commits `47ac071` and `b91d8c2`: inspected before edits.
- `implement-until-working` skill and its one-cycle constraint: reviewed before edits.

## Hypothesis
The product already preserves the submitted question in the new node's origin. Selecting `.node` at index `nodeCountBefore` after the exact-one-node wait and asserting that card's `.origin-quote` avoids coupling the test to the answer title lifecycle. The existing subsequent request-count wait and second-body assertion then independently prove the provider received the full text.

## Changes Made
- Replaced the transient `.node-title` lookup with an exact `.origin-quote` assertion on the newly appended card.
- Kept the wait for the second provider request and its full-text body assertion unchanged.
- No production or generated bundle code changed in this iteration.

## Files Changed
- `test/e2e/followup-ime.test.mjs`
- `implement-until-working/method-history.md`
- `implement-until-working/iterations/003.md`

## Commands Run
```bash
node test/e2e/followup-ime.test.mjs
npm run check:dist
npm run check:types
npm run check:purity
npm test
git diff --check
```

## Restart / Runtime Action
- No long-lived process required restarting.
- The narrow and full E2E runs each launched fresh local browser/server fixtures through the repository harness.

## Verification
- `node test/e2e/followup-ime.test.mjs`: exited 0 with `ok follow-up IME: composing Enter is ignored and completed Korean text submits once in reader and card composers`.
- `npm run check:dist`: exited 0.
- `npm run check:types`: exited 0.
- `npm run check:purity`: exited 0.
- `npm test`: exited 0. Unit, contract, integration, E2E, performance, and packaging suites passed; the registered follow-up IME test passed inside `test:e2e`.
- `git diff --check`: exited 0 after writing this record.

## Acceptance Criteria Checked
- Reader composing Enter makes no request/branch: passed.
- Reader normal Enter submits exactly one complete `테스트`: passed in UI and captured provider body.
- Card composing Enter makes no request/branch: passed.
- Card normal Enter adds exactly one branch retaining `테스트`: passed via the new card's exact `.origin-quote`.
- Card provider request contains complete `테스트`: passed via the second captured request body.
- Shift+Enter remains a newline path: the production condition still excludes shifted Enter and the full suite passes.

## Result
- Result classification: complete.
- The user-visible IME duplication bug is covered in both follow-up composer paths and all required verification is green.
- No merge or push was performed by this iteration.
