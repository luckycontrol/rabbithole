# Iteration 002

## Summary
- Status: progress
- Goal: Make the real-browser regression reach the card IME behavior and include it in the normal E2E suite.
- Why this iteration: Iteration 001 proved the reader fix but the card setup timed out because `#viewport` intercepted a pointer click on the transformed card handle.
- Method: Repository-established keyboard disclosure setup (`focus()` + Enter + textarea-focus wait).
- Compared with the previous baseline: better.

## Context Read
- `AGENTS.md`: reviewed before edits.
- `implement-until-working/task.md`: reviewed before edits.
- `implement-until-working/method-history.md`: reviewed before edits.
- `implement-until-working/iterations/001.md`: reviewed before edits.
- Commit `47ac071`: inspected before edits.
- `implement-until-working` skill and its one-cycle constraint: reviewed before edits.

## Hypothesis
The prior timeout was solely a pointer-action setup problem. Opening the root card drawer via the same keyboard path already exercised in `web-app-canvas-sharing.test.mjs` would avoid canvas hit-testing and let the regression directly dispatch composing and normal Enter events to the card textarea.

## Changes Made
- Replaced the card handle pointer click with focus, keyboard Enter, and an explicit wait for textarea focus.
- Added `test/e2e/followup-ime.test.mjs` to the `test:e2e` script so `npm test` will normally exercise the regression.
- No production or generated bundle code changed.

## Files Changed
- `test/e2e/followup-ime.test.mjs`
- `package.json`
- `implement-until-working/method-history.md`
- `implement-until-working/iterations/002.md`

## Commands Run
```bash
node test/e2e/followup-ime.test.mjs
git diff --check
```

## Restart / Runtime Action
- No long-lived process required restarting.
- The narrow test launched a fresh local static server, browser, and context through `bootWebApp()`.

## Verification
- `node test/e2e/followup-ime.test.mjs`: exited 1, but advanced beyond the previous card-handle timeout.
- Reader composing Enter still made no request and completed `테스트` still produced its single branch, because execution reached the later card assertions.
- Card composing Enter passed both assertions: node count remained unchanged and provider request count remained 1.
- Card completed submission passed the exact-one-node assertion: node count became `nodeCountBefore + 1`.
- The run then failed at `card follow-up title must use the complete composed text`: the locator found 0 `.node-title` elements containing `테스트`.
- Source inspection explains that failure: `sendFollowup()` initially sets the optimistic title from the question, but `handleServer(node_answered)` intentionally replaces it with the provider's title; the zero-delay mock returns `TITLE: Card IME`, so the assertion races the expected title replacement.
- Because the narrow test was not green, the conditional `check:dist`, `check:types`, `check:purity`, and full `npm test` steps were not run in this iteration.
- `git diff --check`: passed before the handoff record was added and is rerun after this record below.

## Acceptance Criteria Checked
- Reader composing Enter makes no request/branch: passed to the extent shown by execution reaching card setup.
- Reader normal Enter submits one complete `테스트`: passed to the extent shown by execution reaching card setup and request count being exactly 1 before the card submit.
- Card composing Enter makes no request/branch: passed directly.
- Card normal Enter creates exactly one branch: passed directly.
- Card request contains complete `테스트`: not reached because the answer-title assertion occurs first.
- Shift+Enter remains a newline path: unchanged production condition; not separately exercised here.

## Result and Next Move
- Result classification: progress.
- The setup failure is removed and the browser regression now directly proves the card composing Enter no-op and single-node completed submission.
- No new production issue was observed; the remaining failure is a test assertion coupled to a transient optimistic title.
- In the next fresh iteration, assert the question on stable UI/model evidence such as the new card's `.origin-quote`, then wait for request count 2 and assert the second request body contains `테스트`. This must be a separate edit-run-verify cycle.
- No merge or push was performed.
